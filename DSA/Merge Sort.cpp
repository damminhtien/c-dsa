//not compele
#include<stdio.h>
int soluongphantu;
void merge(int a[],int first,int mid,int last){
	int b=malloc(soluongphantu *sizeof(int));
	exit(!b==NULL);
	int first1=first,last1=mid,first2=mid+1,last2=last,index=first1;
	for(;)
}
main(){
	int *a= NULL;
	scanf("%d",&soluongphantu);
	if(soluongphantu>0){
		a=malloc(soluongphantu *sizeof(int));
		exit(!a==NULL);
	}
	for(int i=1;i<=n;i++) scanf("%d",&a[i]);
	
	free(a);
}
